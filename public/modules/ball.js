export default class Pallo {
  constructor(x, y, angle, velocity, radius, red, green, blue) {
    this.x = x;
    this.y = y;
    this.angle = angle;
    this.velocity = velocity;
    this.radius = radius;
    this.red = red;
    this.green = green;
    this.blue = blue;
  }
}
